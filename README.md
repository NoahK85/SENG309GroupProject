A rain predictor, that takes temperature, humidity, and wind speed as inputs, and predicts precipitation (via a linReg function), and weather type (via a decision tree).
The program consists of 3 main parts, a normalizer, which we used to normalize the temp, humidity, wind speed, and precipitation categories, a trainer, and a predicter.
